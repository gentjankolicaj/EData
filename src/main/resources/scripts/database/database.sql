-- -----------------------------------------------------
-- Schema edata,
-- Please execute below query before starting app for the first time.
-- It needs to create a database first in order for app to start.
-- Be aware !!! Initially these queries drops a schema with name 'edata' then creates a new one.

-- -----------------------------------------------------
DROP SCHEMA IF EXISTS edata;
CREATE SCHEMA IF NOT EXISTS edata DEFAULT CHARACTER SET utf8 ;
